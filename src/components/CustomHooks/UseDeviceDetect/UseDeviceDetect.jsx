import React from "react";
const useDeviceDetect = () => {
    const [isMobile, setMobile] = React.useState(false);
    React.useEffect(() => {
        const userAgent=typeof window.navigator===""
    })
}